\ProvidesPackage{tfg}
\typeout{Estilo TFG}

\ProvidesPackage{tfg}
\typeout{Estilo Proyecto Fin de Carrera}

% Codificación utf8x y símbolos del idioma español (ñ, acentos, ...)
% El documento será necesario editarlo en utf-8
\usepackage[spanish, es-tabla]{babel}
\usepackage[utf8x]{inputenc}
%\usepackage[utf8]{inputenc}
\usepackage{morefloats}
\usepackage{float}
\usepackage{eurosym}
\usepackage{fancybox}
\usepackage{multicol}
\usepackage{amsmath}
%\usepackage{subfigure}
\usepackage{subcaption}
\usepackage{ifpdf}
\usepackage{anysize}
\usepackage{longtable}    
\usepackage{tikz-uml}    
\usepackage{caption}
\usepackage{vmargin}
\usepackage{enumitem}

%\usepackage{biblatex}
\usepackage[pagebackref=true]{hyperref}
%\usepackage[colorlinks,bookmarks=false]{hyperref}


% Caption Setup
\captionsetup[longtable]{name=Tabla}

% Configuración de márgenes
\usepackage{setspace}
%\doublespacing
\onehalfspacing
\setlength\textwidth{15cm}
\setlength\textheight{22.5cm}
\setlength\oddsidemargin{3.0cm}
\setlength\evensidemargin{3.0cm}
% La separación entre párrafos...
\parskip=6pt
% Identado en la primera línea del nuevo párrafo...
\parindent=10pt
% Número máximo de niveles de profundidad en las secciones.
\setcounter{secnumdepth}{3}
% Establecer el margen superior para los capitulos
\usepackage{titlesec}
\titleformat{\chapter}[display]{\normalfont\Large\bfseries}{\chaptertitlename\ \thechapter}{20pt}{\LARGE}
\footskip = 35pt

% Cabecera y pie de páginas.
\usepackage{fancyhdr}
\pagestyle{fancy}
\fancypagestyle{numberingStyle}{
  \fancyhf{}
  \fancyfoot[C]{\thepage}
  \renewcommand{\headrulewidth}{0pt} % remove lines as well
  \renewcommand{\footrulewidth}{0pt}
}

% Redefine the plain page style
\fancypagestyle{plain}{
	
  \fancyhf{}
  \fancyfoot[C]{\roman{page}}
  \renewcommand{\headrulewidth}{0pt} % remove lines as well
  \renewcommand{\footrulewidth}{0pt}
}

% Redefine the empty page style
\fancypagestyle{empty}{
	
  \fancyhf{}
  \renewcommand{\headrulewidth}{0pt} % remove lines as well
  \renewcommand{\footrulewidth}{0pt}
}

% Code for creating empty pages
% No headers on empty pages before new chapter
\makeatletter
\def\cleardoublepage{\clearpage\if@twoside \ifodd\c@page
	\fancyhf{}
	\fancyfoot[C]{\roman{page}}
  	\renewcommand{\headrulewidth}{0pt} % remove lines as well
  	\renewcommand{\footrulewidth}{0pt}
\else
    \hbox{}
    \fancyhf{}
  	\renewcommand{\headrulewidth}{0pt} % remove lines as well
  	\renewcommand{\footrulewidth}{0pt}
    \newpage
    \if@twocolumn\hbox{}\newpage\fi\fi\fi}
\makeatother \clearpage{\pagestyle{empty}\cleardoublepage}

% Crear página en blanco
\newcommand{\pageblank}{\newpage \thispagestyle{empty}\mbox{}} 


\newcommand\setItemnumber[1]{\setcounter{enumi}{\numexpr#1-1\relax}}